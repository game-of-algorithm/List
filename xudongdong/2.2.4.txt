是否当且仅当两个输入的子数组都有序时原地归并的抽象方法才能得到正确的结果?证明你的结论，或者给出一个反例。

证明：归并前一个输入子数组中的元素索引前后相对位置在归并后不会发生改变，例如归并前子数组1中的元素次序为a1,a2,a3...an,那么在归并后仍就会是a1...a2...a3...an...的次序。如果子数组在归并前无序，那么归并后仍将无序。

反例：当子数组1的元素值为4和3，子数组2的元素为1时，归并会将1放在第1个位置，之后由于子数组2为空，归并会将子数组1的元素按索引从小到大将元素值4和3放在放在第2个和第3个位置，得到排序后的结果1,4,3。显然此排列未按非递减排序。